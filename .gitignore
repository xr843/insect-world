node_modules/
dist/
*.local
.DS_Store
.vite/

# wrangler pages dev 的本地缓存
.wrangler/

# 目视验收渲染图（scripts/new-species-shots.mjs 的输出），只看不提交
audit-out/

# 并行 agent 的一次性校验脚本，不该进仓库
_tmp*.mjs
.tmp-*.mjs
scripts/_*.mjs

# 并行 agent 的 git worktree 挂载点
.claude/worktrees/

# subagent-driven-development 的任务简报与台账，不进仓库
.superpowers/

# 实拍图的下载缓存与产物：图片字节从不进 git（仓库是 MIT，而部分照片禁商用，
# 理由见 scripts/fetch-photos.mjs 的文件头）。清单 src/data/photos.json 才进。
.photo-cache/
public/photos/
