这三张摄于早期版本，界面外壳（左侧竖排工具条、黄色便签、自动旋转药丸）与配色
（复刻自参考站的奶油纸底 + 珊瑚）都已不存在，因此从 README 里撤下，留档备查。

重拍需要 3D 场景真正渲染出来，而 3D 只在浏览器窗口**可见**时才会挂载
（隐藏标签页没有 requestAnimationFrame，canvas 停在 300×150 默认尺寸）。
等窗口可用时重拍并放回 README 即可；图鉴库那张（05-gallery.jpg）是纯 DOM，
已按新配色重拍。
