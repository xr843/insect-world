# Cloudflare Pages 的响应头配置。Vite 会把 public/ 原样复制到 dist 根目录。
#
# CSP 各指令的依据（改动前先核对，否则很容易把站点自己拦死）：
# - script-src 'self'：全部脚本都是同源打包产物，页面上没有内联 <script>
# - style-src 带 'unsafe-inline'：React 的 style={{...}} 内联样式属性需要它，
#   Google Fonts 的样式表来自 fonts.googleapis.com
# - font-src：字体文件从 fonts.gstatic.com 下发；大陆访客拿不到时走系统兜底
# - img-src 加 data:：three.js 内部生成的贴图会用 data: URI
# - connect-src 'self'：物种模型是同源代码分块，站点不请求任何外部接口
# - frame-ancestors 'self'：与 X-Frame-Options SAMEORIGIN 等价的现代写法，两个都留

/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  X-Frame-Options: SAMEORIGIN
  Permissions-Policy: geolocation=(), microphone=(), camera=()
  Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src https://fonts.gstatic.com; img-src 'self' data:; connect-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'self'

# 产物文件名带内容哈希，改了内容就换名字，可以永久缓存
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# HTML 每次都回源校验，保证发新版后用户立刻拿到
/*.html
  Cache-Control: public, max-age=0, must-revalidate

# 物种壳页走美化地址（/s/<id>/ 不带 .html 后缀），上面那条按后缀的规则
# 兜不住它们，按路径再声明一遍同样的回源校验。两棵子树下只有壳页 HTML。
/s/*
  Cache-Control: public, max-age=0, must-revalidate

/en/s/*
  Cache-Control: public, max-age=0, must-revalidate
